死锁：程序执行需要获得两个及以上的锁时，程序经常会碰到死锁的情况：
1，线程之间互相持有对方需要的锁。
2，在一个同步方法中调用一个未知的外部方法，如果这个方法有可能获得另一个锁的化，
    那么可能会引起死锁问题(参考synchronized/listener)。


当Sychronized程序出现死锁时，我们无法打断，除了杀掉JVM。
有一个简单的原则，保证你永远不会碰到死锁：以一个固定的全局的顺序获取锁。