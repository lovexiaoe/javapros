线程池的参数有coreSize：核心线程数，一般和cpu的核数接近。
               maximumPoolSize：最大线程数，
               keepAliveTime，链接存活时间，
               workQueue，工作队列。
               Handler：拒绝策略处理器。

线程执行的顺序为 coreSize,workQueue,maximumPoolSize,handler（丢弃或者异常）。

使用线程池要注意的问题：1，合理连接数的设置，2，线程之间逻辑正确性，避免出现并发问题。3，避免链接泄露（不能释放等）。