模板方法模型使用继承的原理，将重复的工作简化为模板化操作。

使用案例：JDBCTemplete。
对于每个数据库操作，都需要执行一个动作。如果代码设计按照每一个操作的方式来写，那么会产生大写代码，难以维护。
可以将这些操作分类，形成针对于某一特定操作的模板。将操作的元素以参数形式传给模板 。
程序员应该遵从DRY(Don't Repeat Yourself)的原则设计程序。

模板方法模型解决了类冗余的问题，但是也有可能引入类的泛滥。